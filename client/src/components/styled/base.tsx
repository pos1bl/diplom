import { styled } from '@mui/material';

export const StyledActionBar = styled('div')({
  gap: '10px',
  width: '100%',
  display: 'flex',
  padding: '5px 10px',
  alignItems: 'center',
  borderRadius: '20px',
  backgroundColor: '#fff',
  justifyContent: 'center'
});